import hours from "./hours";

export const HOURS = hours;

export const PRICING_TYPE = {
    from: "from",
    fixed: "fixed",
    range: "range",
    perUnit: "per_unit",
}

export const ADDON_SELECTION_TYPE = {
    multi: "multi_select",
    single: "single_select",
}
